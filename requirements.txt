# Core dependencies
# webrtcvad imports pkg_resources, removed in setuptools 82.
setuptools<82
pip>=23.3; sys_platform == 'darwin'
wheel; sys_platform == 'darwin'
PyYAML; sys_platform == 'darwin'
numpy==1.26.4
requests>=2.31.0,<2.32.0
tqdm
wget

# Audio processing
ffmpeg-python>=0.2.0
faiss-cpu==1.7.3
librosa==0.11.0
resampy==0.4.3
scipy==1.11.1
soundfile==0.12.1
noisereduce
pedalboard
stftpitchshift
soxr

# Machine learning and deep learning
omegaconf>=2.0.6; sys_platform == 'darwin'
numba; sys_platform == 'linux'
numba==0.61.0; sys_platform == 'darwin' or sys_platform == 'win32'
torch==2.7.1; sys_platform == 'darwin'
# Previous Windows/Linux build (kept for rollback)
# torch==2.7.1+cu128; sys_platform == 'linux' or sys_platform == 'win32'
torch==2.13.0+cu132; sys_platform == 'linux' or sys_platform == 'win32'
# Windows torch.compile requires Triton separately; 3.7 supports PyTorch 2.13.
triton-windows>=3.7,<3.8; sys_platform == 'win32'
torchaudio==2.7.1; sys_platform == 'darwin'
# torchaudio==2.7.1+cu128; sys_platform == 'linux' or sys_platform == 'win32'
# TorchAudio 2.11 is the final release and supports future PyTorch versions.
torchaudio==2.11.0; sys_platform == 'linux' or sys_platform == 'win32'
torchvision==0.22.1; sys_platform == 'darwin'
# torchvision==0.22.1+cu128; sys_platform == 'linux' or sys_platform == 'win32'
torchvision==0.28.0+cu132; sys_platform == 'linux' or sys_platform == 'win32'
torchcrepe @ git+https://github.com/es2z/torchcrepe_plus.git
torchfcpe
swift_f0
# FCNF0++ (PENN). penn imports torbi at package import, even for argmax decoding.
# The PyPI torbi 1.4.0 Windows wheel ships binaries only up to torch 2.11 / cu130, so
# Windows installs the unmodified build for torch 2.13 / cu132 in vendor/torbi.
# torchutil (a penn dependency) imports apprise and psutil at import time.
penn==1.0.0
vendor/torbi/torbi-1.4.0-cp39-abi3-win_amd64.whl; sys_platform == 'win32'
torbi==1.4.0; sys_platform != 'win32'
einops
# CodenameRingFormer's Conformer blocks use lucidrains' RingAttention. Pure python, and
# with torch.distributed uninitialised it degrades to ordinary causal attention.
# Pinned: ConformerBlock relies on its use_cuda_kernel / force_regular_attn arguments and
# on what the code paths behind them do; tests/test_codename_ringformer.py passes on this
# release.
ring-attention-pytorch==0.5.20
transformers==4.44.2

# Visualization and UI
matplotlib==3.7.2
tensorboard
gradio==5.23.1

# Miscellaneous utilities
certifi>=2023.07.22; sys_platform == 'darwin'
antlr4-python3-runtime==4.8; sys_platform == 'darwin'
tensorboardX
edge-tts==7.2.0
pypresence
beautifulsoup4
sounddevice
webrtcvad
loudness
