# FCNF0++ 既知の問題と音質調整メモ

## 現時点の結論

「音がかすれる／ザラつく」症状については、FCNF0++ が返す音高そのものよりも、`periodicity`（有声音らしさ）を単一のしきい値 `0.065` で0 Hzへ落としている処理が第一候補です。

同じ検証音源では、Mangio-CREPEとの共通有声区間に明らかなオクターブ誤りはありませんでした。一方、FCNF0++の有声フレームは少なく、有声／無声が細かく切り替わっています。したがって、まず `0.065` を `0.050` に下げて比較するのが妥当です。

ただし、これは公式サンプルでの測定結果に基づく仮説であり、実際に症状が出た音源での確定診断ではありません。

また、FCNF0++とは別に、リアルタイム経路のcoarse F0量子化に単位の不一致と思われる既存コードがあります。全F0方式に影響し得るため、後述の切り分けも行う価値があります。

## 最初に試す設定

比較中は、入力音源、RVCモデル、index rate、Protectなど、しきい値以外の設定を固定してください。

1. 設定画面の `Compile FCNF0++ (experimental)` をOFFにする。
2. ピッチシフトを `0`、AutotuneをOFFにする。
3. [`rvc/lib/predictors/f0.py`](rvc/lib/predictors/f0.py) の `FCNF0PP` クラスにある値を次のように変更する。

   ```python
   PERIODICITY_THRESHOLD = 0.050
   ```

4. アプリを再起動し、同じ音源で `0.065` と比較する。
5. まだかすれる場合は `0.045` を試す。
6. 診断目的でのみ `0.000` も試し、かすれが消えるか確認する。`0.000` は息や環境音まで有声化しやすいため、常用値にはしない。

第一候補は `0.050` です。`0.040` 以下は無声区間の誤検出が急増する可能性があるため、先に `0.050` と `0.045` を評価してください。

## しきい値処理の場所

実装箇所は [`rvc/lib/predictors/f0.py`](rvc/lib/predictors/f0.py) です。

```python
class FCNF0PP:
    PERIODICITY_THRESHOLD = 0.065

    # ...
    pitch[periodicity < self.PERIODICITY_THRESHOLD] = 0.0
```

10 msごとの各フレームについて、`periodicity` がしきい値をわずかに下回るだけでF0が0 Hzになります。境界付近のフレームが連続すると、後段へ渡すF0が「有声→0→有声」と短時間で往復し、かすれ、ザラつき、声の途切れとして聞こえる可能性があります。

現在、この値はUIには公開されていません。値を変更した場合はPythonプロセスを再起動してください。

## 公式サンプルでの感度測定

PENN公式の6.5秒サンプル、公式FCNF0++ checkpoint、50–1100 Hz、10 ms hopで測定しました。全650フレームに対する結果です。これは正解ラベルとの精度評価ではなく、しきい値に対する感度の確認です。

| しきい値 | 有声フレーム | 有声／無声の遷移回数 | 1フレームだけの穴 |
|---:|---:|---:|---:|
| 0.000 | 650 | 0 | 0 |
| 0.020 | 650 | 0 | 0 |
| 0.035 | 650 | 0 | 0 |
| 0.050 | 463 | 52 | 12 |
| **0.065（現行）** | **378** | **70** | **7** |
| 0.080 | 338 | 66 | 3 |
| 0.100 | 308 | 56 | 3 |

`periodicity` の分位点は次の通りでした。

| 分位点 | periodicity |
|---:|---:|
| 1% | 0.04053 |
| 5% | 0.04053 |
| 10% | 0.04053 |
| 25% | 0.04443 |
| 50% | 0.08740 |
| 75% | 0.56287 |
| 90% | 0.57910 |
| 95% | 0.58203 |
| 99% | 0.58789 |

現行の `0.065` は、この音源では判定が大きく変わる範囲にあります。`0.050` にすると有声フレームが378から463へ増え、遷移は70から52へ減りました。一方で、`0.035` は全フレームを有声にするため低すぎます。`0.040` 付近も息や無音を拾うリスクがあります。

## Mangio-CREPEとのF0比較

同じ650フレームの公式サンプルで、Mangio-CREPE fullと現行しきい値のFCNF0++を比較した結果です。

| 項目 | 結果 |
|---|---:|
| Mangio-CREPE有声フレーム | 569 |
| FCNF0++有声フレーム | 378 |
| 共通有声フレーム | 375 |
| 共通有声区間の絶対誤差中央値 | 15.2 cents |
| 共通有声区間の絶対誤差95%点 | 97.0 cents |
| 600 cents超のオクターブ誤り | 0 |
| Mangio-CREPE有声F0範囲 | 175.8–938.1 Hz |
| FCNF0++有声F0範囲 | 170.8–954.9 Hz |

共通有声区間の音高追跡は大きく壊れていません。現時点では、オクターブ誤りよりも有声／無声判定の差を先に疑うべき結果です。

## リアルタイム経路にある別の要確認点

[`rvc/realtime/pipeline.py`](rvc/realtime/pipeline.py) のcoarse F0変換は、`f0_mel` に対してHz単位の `self.f0_min` / `self.f0_max` を直接使用しています。

```python
f0_mel = 1127.0 * torch.log(1.0 + f0 / 700.0)
f0_mel = torch.clip(
    (f0_mel - self.f0_min) * 254 / (self.f0_max - self.f0_min) + 1,
    1,
    255,
)
```

オフライン推論と学習時は、`f0_mel_min` / `f0_mel_max` を使っています。リアルタイムの現行式では、上限の1680 Hzを入力してもcoarse値が約208にしかならず、通常期待される255に到達しません。

| 入力F0 | リアルタイム現行式 | mel範囲で量子化した場合 |
|---:|---:|---:|
| 50 Hz | 5 | 1 |
| 100 Hz | 17 | 15 |
| 200 Hz | 37 | 41 |
| 440 Hz | 79 | 93 |
| 880 Hz | 136 | 165 |
| 1100 Hz | 159 | 194 |
| 1680 Hz | 208 | 255 |

これはFCNF0++固有ではなく、リアルタイムの全F0方式に影響します。次のように切り分けてください。

- FCNF0++だけがMangio-CREPEよりかすれる: 先に `PERIODICITY_THRESHOLD` を調整する。
- Mangio-CREPEを含めリアルタイムだけがオフラインより悪い: coarse F0量子化の修正を優先候補にする。
- 高い声ほど差が大きい: coarse F0量子化と `f0_max` の影響を特に疑う。

この文書追加時点では、既存方式の挙動を変えないため、この量子化コード自体は変更していません。

## ほかに試す価値があるパラメータ

### Protect

リアルタイムUIの `Protect Voiceless Consonants` は初期値 `0.5` です。子音境界やブレスでだけザラつく場合は、まず `0.5` のまま確認し、その後 `0.33` と比較してください。

- `0.5`: 無声子音とブレスの保護を強くする。境界のかすれには有利な可能性がある。
- `0.33`: index由来の影響とのバランス比較用。

しきい値とProtectを同時に変えると原因を判定できないため、必ず一つずつ変更します。

### f0_min / f0_max

現在の範囲は経路により異なります。

- オフライン推論: [`rvc/infer/pipeline.py`](rvc/infer/pipeline.py) の50–1100 Hz
- リアルタイム推論: [`rvc/realtime/pipeline.py`](rvc/realtime/pipeline.py) の50–1680 Hz
- 学習用F0抽出: [`rvc/train/extract/extract.py`](rvc/train/extract/extract.py) の50–1680 Hz

しきい値の検証中は変更しないでください。声域の上端付近だけで問題が出る場合に、学習時の範囲と推論時の範囲をそろえたA/Bテストを行います。一つの経路だけを1100 Hzへ変更すると別の差を作るため、モデルの学習条件を確認してから行うべきです。

### Autotune、ピッチシフト、index

- Autotune: 音高追跡の素の状態を確認する間はOFF。
- ピッチシフト: 診断中は0半音。
- index rate: 比較中は固定。可能なら0でも確認し、F0由来かindex由来かを分離する。
- decoder: `viterbi` のままにする。現在の比較では明らかなオクターブ誤りを抑えられている。
- hop size / center: 10 ms / half-hopのままにする。RVCの `p_len` と直接一致させるため変更しない。
- `BATCH_SIZE`: 速度／メモリ用であり、かすれ調整のパラメータとしては使わない。

## torch.compileについて

音質比較の基準はeager（compile OFF）にしてください。torch.compileは音質を改善する機能ではなく、固定shapeの反復推論を高速化できる場合にだけ使います。

この環境で、1.07秒窓をずらしながら100回継続推論したF0推定器部分の測定は次の通りでした。

| FCNF0++ | 継続推論平均 | p95 |
|---|---:|---:|
| eager | 29.80 ms | 34.01 ms |
| compiled | 28.87 ms | 30.36 ms |

compiledはF0部分のp95を少し改善しましたが、初回compileコストが大きく、RVC全体では必ずしも速くなりませんでした。リアルタイムで長時間使う場合でも、実際のブロックサイズでeagerより継続p95が改善したときだけ有効化してください。音質診断中はOFFにして変数を減らします。

## 推奨A/Bテスト手順

1. 同じ入力、同じRVCモデル、同じindex rate、ピッチシフト0、Autotune OFF、compile OFFにする。
2. `PERIODICITY_THRESHOLD` を `0.065`、`0.050`、`0.045` の順で録音する。
3. 必要なら `0.000` を診断専用で録音する。
4. 次の箇所を別々に確認する。
   - 持続母音: 短い音切れ、ザラつき、F0の揺れ
   - 小声／息混じり声: 有声判定の欠落
   - 子音境界: 破裂音や摩擦音のにじみ、電気的なかすれ
   - ビブラート／音程移動: オクターブ飛び、直線的な段差
   - 無音／ブレス: 低すぎるしきい値による偽の音高
5. 最良のしきい値を固定した後で、Protect `0.5` と `0.33` を比較する。
6. FCNF0++だけでなくMangio-CREPE fullも同条件で録音する。

ベンチマークは次のコマンドで実行できます。現在、しきい値はコマンドライン引数ではないため、各測定前に定数を変更してアプリ／Pythonプロセスを再起動します。

```powershell
env\python.exe -m rvc.lib.predictors.benchmark_f0 `
  --audio "<input.wav>" `
  --methods "mangio-crepe-full-eager,fcnf0++" `
  --iterations 100
```

## しきい値だけで直らない場合の実装修正候補

単一しきい値を下げても「誤って息を有声化する」問題との両立が難しい場合は、次の順で検討します。

1. **ヒステリシス**: 有声開始と有声終了で別のしきい値を使う。例として開始 `0.06`、維持 `0.04` など。ただし実音源の分布を測ってから決める。
2. **1フレーム穴の除去**: 前後が有声なら10 msだけの0 Hzを埋める。長い無声区間は維持する。
3. **最小ラン長**: 20–30 ms未満の有声／無声反転だけを抑制する。
4. **UI設定化**: 十分な比較後、`PERIODICITY_THRESHOLD` をリアルタイムUIで調整できるようにする。

全面的な時間補間は子音や本当の無声区間を壊すため避けます。短い穴だけを局所的に処理し、最終F0長が常に `p_len` と一致する既存保証は維持します。

## 合格条件

- 最終F0配列長が常に `p_len` と一致する。
- NaN / Infがなく、0 Hzまたは `f0_min`–`f0_max` 内に収まる。
- 持続母音中の短い0 Hzと有声／無声遷移が減る。
- 無音、息、摩擦音に偽の安定F0を増やさない。
- Mangio-CREPE比較で明らかなオクターブ誤りを増やさない。
- 実際のリアルタイムブロックで継続推論のp95が処理期限内に収まる。

## 現在残っている問題

- ユーザー音源の`periodicity`分布と、かすれが発生した時刻の対応はまだ未測定。
- しきい値がコード定数で、UIから変更できない。
- 単一しきい値なので境界付近でチャタリングし得る。
- リアルタイムcoarse F0量子化がオフライン／学習経路と一致していない。
- torch.compileは利用可能だが、ブロックサイズやCUDA Graphsとの相性により継続速度が変わるため、常時高速とは限らない。

